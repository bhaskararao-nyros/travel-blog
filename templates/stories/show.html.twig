{% extends 'base.html.twig' %}

{% block title %} {{ story.title }} {% endblock %}

{% block body %}

	<div class="mt-3">
		<div class="card mt-3 border-default">
		  	<div class="card-body">
		    	<h5 class="card-title">{{ story.title }} <small class="text-muted"><i>- {{ story.createdBy }}</i></small></h5>
			    <p class="card-text">{{ story.description }}</p>
			    <a href="/"><< Go Back</a>
		  	</div>
		</div>
	</div>
{% endblock %}